spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/quizTest
    username: postgres
    password: postgres
  liquibase:
    change-log: classpath:db/changelog/changelog-master.xml
    drop-first: true
  jpa:
    hibernate:
      ddl-auto: none
jwt:
  secret: qwe
